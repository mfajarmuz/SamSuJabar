// src/config.js
// Android emulator: 10.0.2.2 maps to host machine localhost
// Physical device: replace with your machine's local IP (run ipconfig, use IPv4)
const API_URL = 'https://sam-su-jabar.vercel.app';

export default API_URL;
