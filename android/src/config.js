// src/config.js
// Production APK uses the deployed Vercel backend so it can work
// without depending on a local Tailscale-connected development server.
const API_URL = 'https://sam-su-jabar.vercel.app';

export default API_URL;
